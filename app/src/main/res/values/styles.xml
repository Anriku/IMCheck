<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Popupwindow">
        <item name="android:windowEnterAnimation">@anim/window_out</item>
        <item name="android:windowExitAnimation">@anim/window_back</item>
    </style>

    <style name="Theme.ActivityDialogStyle" parent="Theme.Design.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowCloseOnTouchOutside">false</item>
        <item name="android:windowIsFloating">true</item>
    </style>

    <!--TextSize-->
    <dimen name="text_size">20sp</dimen>
    <dimen name="text_size_small">15sp</dimen>
    <dimen name="text_size_max">25sp</dimen>
    <!--Margin Or Padding-->
    <dimen name="margin">10dp</dimen>
    <dimen name="margin_small">5dp</dimen>
    <dimen name="margin_large">15dp</dimen>
    <!--Radius-->
    <dimen name="radius">10dp</dimen>
    <dimen name="radius_small">5dp</dimen>

    <!--PicSizeAndMargin-->
    <dimen name="size_pic">40dp</dimen>
    <dimen name="size_card">40dp</dimen>
    <dimen name="pic_margin">15dp</dimen>

    <!--portrait-->
    <dimen name="portrait_size">60dp</dimen>


</resources>
